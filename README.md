This project replicates how a read heavy DB behaves. Most of the reads are from replica and writes are to the master DB. This program is created to show the routing logic hidden under a managed Amazon Service

